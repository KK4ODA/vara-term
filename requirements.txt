PyQt6>=6.5
cryptography>=41.0
pyserial>=3.5
certifi>=2023.0  # SSL certificates for Hamlib auto-download
keyring>=24.0  # optional — OS-level credential storage
pywin32>=306   # optional — required for OmniRig HF PTT (Windows only)
